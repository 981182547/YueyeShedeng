# 车辆图片

2.0 用的是 `newcar.png`（2730×1535，那张标了 1~8 号灯的红色 Jeep）。
`pubspec.yaml` 里只声明了这一张，同目录的 `car.png` 是 4 组版留下的，不会被打包。

## 灯位对不上怎么办

灯位坐标写在 `lib/models/lamp.dart` 的 `kLamps` 表里。
`x` / `y` 是相对坐标（0~1，相对图片左上角），换了别的车图就重新量这张表，
其它代码都不用动。
